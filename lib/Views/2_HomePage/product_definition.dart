import 'package:flutter/material.dart';

class productDefinition extends StatefulWidget {
  const productDefinition({Key? key}) : super(key: key);

  @override
  _productDefinitionState createState() => _productDefinitionState();
}

class _productDefinitionState extends State<productDefinition> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("abasdf"),
    );
  }
}
